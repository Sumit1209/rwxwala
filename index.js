/*function changeBg(){
    var navnar1=document.getElementById("nav")
    var scrollValue=window.scrollY;
  console.log(scrollValue);
  if (scrollValue<630){
    navnar1.classList.remove('bg')
  }
  else{
    navnar1.classList.add('bg')
  }

  }
  window.addEventListener('scroll',changeBg)

*/
 